<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:bind="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="com.android.committeeapp.common.AddViewModel" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="?android:colorBackground"
        android:orientation="vertical">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="?colorPrimary"
            android:elevation="@dimen/spacing_normal"
            android:theme="@style/ThemeOverlay.Expenses.Toolbar"
            app:popupTheme="@style/ThemeOverlay.MaterialComponents.Light" />

        <include
            android:id="@+id/fragment_navigation_host"
            layout="@layout/layout_add_form"
            android:layout_width="match_parent"
            android:layout_height="300dp"
            bind:viewModel="@{viewModel}" />


    </LinearLayout>
</layout>
